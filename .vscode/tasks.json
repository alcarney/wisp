{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build WASM",
            "type": "shell",
            "command": "tinygo build -no-debug -target wasm -o public/js/wisp.wasm main.go",
            "group": "build",
            "problemMatcher": [],
        }
    ]
}